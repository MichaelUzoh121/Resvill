import React, { useMemo, useState } from "react";
import { Link, Navigate, useNavigate } from "react-router-dom";
import { CheckCircle2, MapPin, ShieldCheck } from "lucide-react";
import { toast } from "react-hot-toast";
import { formatNaira, useCart } from "../context/CartContext";
import { createOrder } from "../utils/orders";

const savedProfile = () => { try { return JSON.parse(localStorage.getItem("resvill_profile_v1") || "{}"); } catch { return {}; } };
function Checkout() {
  const { items, totals, clearCart } = useCart();
  const navigate = useNavigate();
  const profile = useMemo(savedProfile, []);
  const [deliveryMethod, setDeliveryMethod] = useState("delivery");
  const [form, setForm] = useState({ name: profile.name || "", email: profile.email || "", phone: profile.phone || "", address: profile.address || "", instructions: "" });
  const summary = totals(deliveryMethod);
  if (!items.length) return <Navigate to="/cart" replace />;
  const update = (event) => setForm((current) => ({ ...current, [event.target.name]: event.target.value }));
  const submit = (event) => { event.preventDefault(); if (deliveryMethod === "delivery" && !form.address.trim()) { toast.error("Please enter your delivery address."); return; } const order = createOrder({ customer: form, items, totals: summary, deliveryMethod }); localStorage.setItem("resvill_profile_v1", JSON.stringify(form)); clearCart(); navigate(`/order-confirmation/${order.id}?token=${order.trackingToken}`); };
  return <main className="bg-dark-50/50 py-10 sm:py-14"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><p className="text-sm font-bold uppercase tracking-widest text-primary-500">Secure checkout</p><h1 className="mt-2 font-heading text-3xl font-extrabold text-dark-950">Complete your order</h1><div className="mt-8 grid gap-8 lg:grid-cols-[1fr_380px]"><form onSubmit={submit} className="space-y-6"><section className="rounded-2xl border border-dark-100 bg-white p-5 shadow-soft sm:p-6"><h2 className="font-heading text-xl font-bold text-dark-950">Contact details</h2><p className="mt-1 text-sm text-dark-500">Checkout as a guest. You can create an account later with this email.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold text-dark-700">Full name<input required name="name" value={form.name} onChange={update} className="mt-2 w-full rounded-xl border border-dark-200 px-4 py-3 font-normal outline-none focus:border-primary-500" /></label><label className="text-sm font-semibold text-dark-700">Email<input required type="email" name="email" value={form.email} onChange={update} className="mt-2 w-full rounded-xl border border-dark-200 px-4 py-3 font-normal outline-none focus:border-primary-500" /></label><label className="text-sm font-semibold text-dark-700">Phone<input required name="phone" value={form.phone} onChange={update} className="mt-2 w-full rounded-xl border border-dark-200 px-4 py-3 font-normal outline-none focus:border-primary-500" /></label><label className="text-sm font-semibold text-dark-700">Delivery method<select name="deliveryMethod" value={deliveryMethod} onChange={(event) => setDeliveryMethod(event.target.value)} className="mt-2 w-full rounded-xl border border-dark-200 bg-white px-4 py-3 font-normal outline-none focus:border-primary-500"><option value="delivery">Home delivery</option><option value="pickup">Pickup at Resvill</option></select></label></div>{deliveryMethod === "delivery" && <label className="mt-4 block text-sm font-semibold text-dark-700">Delivery address<textarea required name="address" value={form.address} onChange={update} rows="3" placeholder="Street, area, city" className="mt-2 w-full rounded-xl border border-dark-200 px-4 py-3 font-normal outline-none focus:border-primary-500" /></label>}<label className="mt-4 block text-sm font-semibold text-dark-700">Special instructions <span className="font-normal text-dark-400">(optional)</span><textarea name="instructions" value={form.instructions} onChange={update} rows="2" placeholder="Anything the kitchen should know?" className="mt-2 w-full rounded-xl border border-dark-200 px-4 py-3 font-normal outline-none focus:border-primary-500" /></label></section><section className="rounded-2xl border border-dark-100 bg-white p-5 shadow-soft sm:p-6"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-full bg-primary-50 text-primary-500"><ShieldCheck size={21} /></div><div><h2 className="font-heading text-xl font-bold text-dark-950">Mock payment</h2><p className="text-sm text-dark-500">Paystack will be connected here later. This test payment is not charged.</p></div></div><button type="submit" className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-primary-500 px-5 py-3.5 font-bold text-white hover:bg-primary-600"><CheckCircle2 size={18} /> Pay {formatNaira(summary.total)} securely</button></section></form><aside className="h-fit rounded-2xl border border-dark-100 bg-white p-5 shadow-soft sm:p-6"><h2 className="font-heading text-xl font-bold text-dark-950">Order Summary</h2><div className="mt-5 space-y-4">{items.map((item) => <div key={item.id} className="flex justify-between gap-3 text-sm"><span className="text-dark-600">{item.quantity} × {item.name}</span><span className="font-semibold text-dark-900">{formatNaira(item.unitPrice * item.quantity)}</span></div>)}</div><div className="mt-5 space-y-3 border-t border-dark-100 pt-5 text-sm"><div className="flex justify-between text-dark-600"><span>Subtotal</span><span>{formatNaira(summary.subtotal)}</span></div><div className="flex justify-between text-dark-600"><span>{deliveryMethod === "pickup" ? "Pickup" : "Delivery"}</span><span>{formatNaira(summary.deliveryFee)}</span></div><div className="flex justify-between text-dark-600"><span>Tax</span><span>{formatNaira(summary.tax)}</span></div><div className="flex justify-between border-t border-dark-100 pt-4 text-base font-extrabold"><span>Total</span><span>{formatNaira(summary.total)}</span></div></div><p className="mt-5 flex gap-2 text-xs leading-5 text-dark-500"><MapPin size={15} className="shrink-0 text-primary-500" /> Your tracking link will be generated immediately after payment.</p><Link to="/cart" className="mt-5 block text-center text-sm font-bold text-primary-500">Return to cart</Link></aside></div></div></main>;
}
export default Checkout;
